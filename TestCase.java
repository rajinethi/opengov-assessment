package page_objects;

import org.openqa.selenium.WebDriver;

public class TestCase {

	public static void Login_Entry(WebDriver driver, String uid, String pwd)
	{
		//TODO: For not hard coding the values.
//		PageElement.SignIn_username_editbox(driver).sendKeys(uid);
//		PageElement.password_username_editbox(driver).sendKeys(pwd);
//		PageElement.SigIn_button(driver).click();
		
	}
	
}
